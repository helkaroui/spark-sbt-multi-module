app-name = "MySparkApp-reference"

spark-config = {
  "spark.serializer" = "org.apache.spark.serializer.KryoSerializer",
  "spark.sql.parquet.compression.codec" = "snappy",
  "spark.hadoop.fs.s3a.fast.upload" = "true"
}

application = {
  save-path = ""
}